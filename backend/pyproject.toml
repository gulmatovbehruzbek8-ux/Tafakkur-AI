[project]
name = "backend"
version = "0.1.0"
description = "Tafakkur AI Backend API"
requires-python = ">=3.9"
dependencies = [
    "fastapi",
    "uvicorn",
    "pydantic",
]

[tool.vercel]
entrypoint = "main:app"
